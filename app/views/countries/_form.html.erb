<%= form_with(model: country, local: true) do |form| %>
  <% if country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
      <% country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Country code: <%= form.text_field :country_code %>
  </div>

  <div class="field">
    English: <%= form.text_field :name_en %>
  </div>

  <div class="field">
    Spain: <%= form.text_field :name_es %>
  </div>
  
  <div class="field">
    VAT: <%= form.number_field :tax_percent, :step => 0.001 %>
  </div>
  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
