<%= form_for(@upload) do |f| %>
	<%= f.file_field :data  %>
	<%= f.collection_select :main_id, @mains, :id, :data, {include_blank: true, :selected => @upload.main_id}, {:style => "width: 190px;"} %>
	<%= f.submit 'Save' %>
<% end %>