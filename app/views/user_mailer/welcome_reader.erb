Hi,

<p>Thank you for joining The Spain Report. Please click on this link to confirm your email address and set a password:</p>

<p><%= link_to "Confirm email address", confirm_email_user_url(@user.confirm_token) %></p>

<p>
    Latest podcast episodes:
    <ul>
        <% @latestpodcasts.each do |a| %>
            <li><%= link_to a.headline, a %></li>
        <% end %>
    </ul>
    
    Latest Spain articles:
    <ul>
        <% @latestfree.each do |a| %>
            <li><%= link_to a.headline, a %></li>
        <% end %>
    </ul>
    
    Latest Truth & Journalism articles:
    <ul>
        <% @latesttruth.each do |a| %>
            <li><%= link_to a.headline, a %></li>
        <% end %>
    </ul>
    
    Latest patrons only:
    <ul>
        <% @latestpatrons.each do |a| %>
            <li><%= link_to a.headline, a %></li>
        <% end %>
    </ul>
</p>

<p><%= link_to @user.frame.emotional_quest_action, root_url + "value/" + @user.frame.link_slug %></p>

<p>If you did not request this e-mail, just ignore it.</p>

<p>Kindest regards,</p>

<p>
    Matthew Bennett
    <br />
    The Spain Report
</p>